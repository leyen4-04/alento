import React from 'react';

// 임시 헤더 컴포넌트
function Header() {
  return (
    <header>
      {/* 이 헤더는 현재 사용되고 있지 않지만,
          오류를 없애기 위해 기본 코드를 추가합니다. */}
    </header>
  );
}

// export default 구문이 있어야 모듈로 인식됩니다.
export default Header;

